# Matter Overdrive 1.20.1 - Milestone 2 current verification

The current build is `0.8.0.0-alpha.4.1`.

Use `M2_NETWORK_VERIFICATION_PATH.md` for the authoritative test path.

Start with:

```bat
VERIFY_M2_BUILD.bat
```

Then run:

```bat
RUN_M2_CLIENT.bat
```

Alpha.4 keeps the verified Decomposer / Recycler / Analyzer / Replicator functionality and adds Pattern Storage, Pattern Monitor, Network Pipe pattern/task transport, and Matter Pipe transport.
